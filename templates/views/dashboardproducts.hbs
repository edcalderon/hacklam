{{>headdashboard}}

<body id="page-top">
  <!-- Page Wrapper -->
  <div id="wrapper">
    <!-- Sidebar-->
    {{>sidebardashboard}}
    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column">
      <!-- Main Content -->
      <div id="content">
        <!-- Topbar -->
        {{>topbar}}
        <!-- End of Topbar -->
        <!-- Begin Page Content -->
        <div class="container-fluid">
          <!-- Page Heading -->
          {{#if registro}}
          <h2 class="text-center" id="title">{{{show}}}</h2>
          {{/if}}
          {{#if administrador}}
          <div class="card-body">
            <div class="limiter">
              <div class="container-table100">
                <div class="wrap-table100">
                  <div class="row">
                    <form action="/dashboardadmin?listar=true" method="post">
                      <div class="form-row">
                        <div class="col "><input type="text" name="busqueda" placeholder="editar por cedula"></div>
                        <div class="col "><button type="submit" class="btn btn-primary"> buscar </button></div>
                      </div>
                    </form>
                  </div>
                  <br></br>
                  {{>usersadmintable}}
                </div>
              </div>
            </div>
          </div>
          {{>noaccess}}
          {{/if}}
          <!-- end Content Row -->
        </div>
        <!-- end container-fluid -->
      </div>
      <!-- End of Main Content -->
      {{>footerdashboard}}
    </div>
    <!-- End of Content Wrapper -->
  </div>
  <!-- End of Page Wrapper -->
  <!-- Footer -->
</body>
</html>